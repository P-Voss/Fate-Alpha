<?php
$this->headScript()->prependFile($this->baseUrl() . '/js/tinymce/tinymce.min.js');
$this->headScript()->prependFile($this->baseUrl() . '/js/Administration/editor.js');
?>
<h1>Fähigkeit erstellen</h1>
<form action="<?= $this->baseUrl() ?>/Administration/skill/create" method="post">
    <fieldset>
        <p>
            <label for="name">Name</label>
            <input type="text" size="30" name="name" />
            <label for="kosten">FP-Kosten</label>
            <input type="number" name="fp" />
        </p>
        <p>
            <label for="skillart">
                Fähigkeitenart
            </label>
            <select name="skillart">
                <option value="1">Schützenfähigkeit</option>
                <option value="2">Kämpferfähigkeit</option>
                <option value="3">Übungsfähigkeit</option>
                <option value="4">Spezialfähigkeit</option>
            </select>
        </p>
        <p>
            <label for="rang">
                Rang
            </label>
            <select name="rang">
                <option>EX</option>
                <option>A</option>
                <option>B</option>
                <option>C</option>
                <option>D</option>
                <option>E</option>
                <option>F</option>
            </select>
        </p>
        <p>
            <label for="uebung">
                benötigte Übung
            </label>
            <input type="number" name="uebung" />
        </p>
        <p>
            <label for="disziplin">
                benötigte Disziplin
            </label>
            <input type="number" name="disziplin" />
        </p>
            <p>
                <label for="staerke">Stärke</label>
                <input type="number" size="30" name="staerke" id="staerke" />
            </p>
            <p>
                <label for="agilitaet">Agilität</label>
                <input type="number" size="30" name="agilitaet" id="agilitaet" />
            </p>
            <p>
                <label for="ausdauer">Ausdauer</label>
                <input type="number" size="30" name="ausdauer" id="ausdauer" />
            </p>
            <p>
                <label for="kontrolle">Kontrolle</label>
                <input type="number" size="30" name="kontrolle" id="kontrolle" />
            </p>
        <label for="gruppen">
            Klassengruppen:
        </label>
        <select name="gruppen[]" multiple="true" size="8">
            <?php foreach ($this->klassengruppen as $gruppe) { ?>
            <option value="<?= $gruppe->getId() ?>">
                <?= $gruppe->getBezeichnung() ?>
            </option>
            <?php } ?>
        </select>
        <br />
        <label for="klassen">
            Klassen:
        </label>
        <select name="klassen[]" multiple="true" size="8">
            <?php foreach ($this->klassen as $klasse) { ?>
            <option value="<?= $klasse->getId() ?>">
                <?= $klasse->getBezeichnung() ?>
            </option>
            <?php } ?>
        </select>
        <p>
            <label for="skills">
                Fähigkeiten:
            </label>
            <select name="skills[]" multiple="true" size="8">
                <?php foreach ($this->skills as $skill) { ?>
                <option value="<?= $skill->getId() ?>">
                    <?= $skill->getBezeichnung() ?>
                </option>
                <?php } ?>
            </select>
        </p>
        <p>
            <label for="skillsIncompatible">
                Nicht kombinierbar mit:
            </label>
            <select name="skillsIncompatible[]" multiple="true" size="8">
                <?php foreach ($this->skills as $skill) { ?>
                <option value="<?= $skill->getId() ?>">
                    <?= $skill->getBezeichnung() ?>
                </option>
                <?php } ?>
            </select>
        </p>
        <p>
            <label for="vorteile">
                benötigte Vorteile
            </label>
            <select name="vorteile[]" multiple="true" size="8">
                <?php foreach ($this->vorteile as $vorteil):
                    $selected = (in_array($vorteil->getId(), $vorteileReq)) ? 'selected=""' : '';
                ?>
                <option value="<?= $vorteil->getId() ?>" <?= $selected ?>>
                    <?= $vorteil->getBezeichnung() ?>
                </option>
                <?php endforeach; ?>
            </select>
        </p>
        <p>
            <label for="nachteile">
                benötigte Nachteile
            </label>
            <select name="nachteile[]" multiple="true" size="8">
                <?php foreach ($this->nachteile as $nachteil):
                    $selected = (in_array($nachteil->getId(), $nachteileReq)) ? 'selected=""' : '';
                ?>
                <option value="<?= $nachteil->getId() ?>" <?= $selected ?>>
                    <?= $nachteil->getBezeichnung() ?>
                </option>
                <?php endforeach; ?>
            </select>
        </p>
        <label for="beschreibung">
            Beschreibung
        </label>
        <textarea name="beschreibung" cols="80" rows="10"></textarea>
    </fieldset>
    <div align="center">
        <input type="submit" name="Abschicken" id="Abschicken" value="speichern" />
    </div>
</form>