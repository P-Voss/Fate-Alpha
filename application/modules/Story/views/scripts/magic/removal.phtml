<?php
/* @var $purifier HTMLPurifier */
$purifier = $this->purifier;
/* @var $magien Story_Model_Magie */
$magien = $this->magien;
?>
<div class="row">
    <div class="col-12 fs-4">
        Magien verlernen
    </div>
    <div class="col-4">
        <?php if(count($magien) > 0){ ?>
            <select class="form-select" id="learnMagien" multiple="" size="5">
                <?php foreach($magien as $magie): ?>
                    <option value="<?= $magie->getId() ?>"><?= $purifier->purify($magie->getBezeichnung()) ?></option>
                <?php endforeach; ?>
            </select>
            <input data-art="magie" data-request="remove"  class="removeMagic btn btn-danger" type="submit" value="Speichern">
        <?php } else { ?>
            Der Charakter kann keine Magien verlernen.
        <?php } ?>
    </div>
</div>