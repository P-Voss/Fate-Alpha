<?php
/* @var $purifier HTMLPurifier */
$purifier = $this->purifier;
/* @var $magien Story_Model_Magie */
$magien = $this->magien;
?>
<fieldset>
    <legend>Magien eintragen</legend>
    <div>
        <?php if(count($magien) > 0){ ?>
            <select id="learnMagien" multiple="" size="5">
                <?php foreach($magien as $magie): ?>
                    <option value="<?= $magie->getId() ?>"><?= $purifier->purify($magie->getBezeichnung()) ?></option>
                <?php endforeach; ?>
            </select>
            <input data-request="remove" class="saveMagic" type="submit" value="Speichern">
        <?php } else { ?>
            Der Charakter kann im Moment keine Magien per RPG erlernen.
        <?php } ?>
    </div>
</fieldset>