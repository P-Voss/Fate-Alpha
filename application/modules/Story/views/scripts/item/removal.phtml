<?php
/* @var $purifier HTMLPurifier */
$purifier = $this->purifier;
/* @var $item Story_Model_Item */
$items = $this->items;
?>
<div class="row">
    <div class="col-12 fs-4">
        Gegenstände entfernen
    </div>
    <div class="col-4">
        <?php if(count($items) > 0){ ?>
            <select class="form-select" id="removeItems" multiple="" size="5">
                <?php foreach($items as $item): ?>
                    <option value="<?= $item->getId() ?>"><?= $purifier->purify($item->getName()) ?></option>
                <?php endforeach; ?>
            </select>
            <input class="removeItems btn btn-danger" type="submit" value="Speichern">
        <?php } else { ?>
            Der Charakter hat keine Items die er abgeben kann.
        <?php } ?>
    </div>
</div>