<fieldset>
    <legend>Traits</legend>
<?php foreach($charakter->getTraits() as $trait): ?>
    <div>
        <legend>
            <?= $purifier->purify($trait->getName()) ?> <span class="indicator"></span>
        </legend>
        <div class="subContent">
            <?= $purifier->purify($trait->getBeschreibung()) ?>
        </div>
    </div>
<?php endforeach; ?>
</fieldset>