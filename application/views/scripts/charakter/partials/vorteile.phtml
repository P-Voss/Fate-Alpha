<fieldset>
    <legend>Vorteile</legend>
<?php foreach($charakter->getVorteile() as $vorteil): ?>
    <div>
        <legend>
            <?= $purifier->purify($vorteil->getBezeichnung()) ?> <span class="indicator"></span>
        </legend>
        <div class="subContent">
            <?= $purifier->purify($vorteil->getBeschreibung()) ?>
        </div>
    </div>
<?php endforeach; ?>
</fieldset>